# Environment Variables Template
# Copy this file to .env.local and fill in your values

# Base URL for your deployment (used for QR codes and redirects)
BASE_URL=https://linksnappy.imstryker.com

# Storage Configuration
# Options: 'json' (default) or 'postgresql'
STORAGE_TYPE=json

# PostgreSQL Configuration (only needed if STORAGE_TYPE=postgresql)
# DATABASE_URL=postgresql://username:password@localhost:5432/linksnappy

# Next.js Configuration
NODE_ENV=production